import {Display} from "./js/Classes/Display";
import {ButtonChange} from "./js/Classes/ButtonChange";

new Display();
const buttonChanger = new ButtonChange();

const add = document.querySelector('.addItemButton');
const clear = document.querySelector('.clear');
const button = document.querySelectorAll('.button');
button.forEach(e => e.add.addEventListener('mousedown',(e) {
    buttonChanger.click(this)
};
for (let i = 0 )
}
